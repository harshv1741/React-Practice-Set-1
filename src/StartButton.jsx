// eslint-disable-next-line react/prop-types
function StartButton({ StyledButton }) {
    return (
        <div>
            <button style={StyledButton}>Start</button>
        </div>
    );
}

export default StartButton;
